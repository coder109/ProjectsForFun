If you have properly installed Chez Scheme, just run:
```bash
scheme.exe --script hanoi.scm
```

The position of `Scheme.exe` will be like this:
```text
/c/Program Files/Chez Scheme 10.1.0/bin/ta6nt/scheme.exe
```